# Backstop only - both paths are already blocked from `chezmoi add` by
# home/.chezmoiignore. This just covers a copy landing in the tree some
# other way (e.g. hand-copied in). Matches on basename only.
*access-token*
.credentials.json

# Local Claude Code project instructions - not for GitHub
/CLAUDE.md

# Personal Claude Code hooks for this project, not shared with the team
/.claude/settings.local.json
